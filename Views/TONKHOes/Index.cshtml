@model IEnumerable<DateTime>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@Html.ActionLink("Tạo mới", "Create")
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Ngày")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.ToString("dd-MM-yyyy") })
        </td>
    </tr>
}

</table>
